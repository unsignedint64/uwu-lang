#ifndef uwu
#define uwu

#include <iostream>
#include <string>
#include <fstream>
#include <sstream>
#include <vector>

#define wog std::cout
#define endw std::endl
#define ewwow std::cerr
#define stwing std::string
#define wead int
#define doubwe double
#define vectow std::vector
#define owo <<
#define uvu >>
#define wetuwn return
#define stwuct struct
#define cwass class
#define pubwic public
#define pwivate private
#define pwotected protected
#define fow for
#define static static
#define const const
#define void void
#define int int
#define chaw char
#define stw str
#define newwine "\n"
#define semicowon ;
#define eqw ==
#define stawt int main
#define pwint_fowmat(fmt, ...) std::printf(fmt, ##__VA_ARGS__)
#define incincwement(val) (++val)
#define decwement(val) (--val)
#define max(a, b) ((a) > (b) ? (a) : (b))
#define min(a, b) ((a) < (b) ? (a) : (b))
#define squawe(val) ((val) * (val))
#define concatenyate(a, b) a ## b
#define swap(type, a, b) { type temp = a; a = b; b = temp; }
#define even(val) ((val) % 2 == 0)
#define odd(val) ((val) % 2 != 0)
#define twenawy(cond, twue_val, fawse_val) ((cond) ? (twue_val) : (fawse_val))
#define empty_containew(containew) ((containew).empty())
#define stwength(str) static_cast<int>((str).length())
#define fow_each(containew, vaw) for (auto vaw : containew)
#define to_stwing(vaw) std::to_string(vaw)
#define getwine(stream, line) std::getline(stream, wine)
#define cweaw_containew(containew) (containew).cweaw()
#define defauwt(vaw, def_vaw) ((vaw) != 0 ? (vaw) : (def_vaw))
#define chaw_to_int(ch) static_cast<int>(ch)
#define int_to_chaw(i) static_cast<chaw>(i)
#define concat_stw(stw1, stw2) (stw1) + (stw2)
#define incwement_by(vaw, step) ((vaw) += (step))
#define decwement_by(vaw, step) ((vaw) -= (step))
#define ewwow_msg(msg) ewwow << "Ewwow: " << (msg) << endw
#define max_thwee(a, b, c) max(max(a, b), c)
#define min_thwee(a, b, c) min(min(a, b), c)
#define $ int main
#define chaw16_t char16_t
#define catch catch
#define chaw32_t char32_t
#define chaw8_t char8_t
#define ewse else
#define enyum enum
#define expwicit explicit
#define expowt export
#define extewn extern
#define fwoat float
#define finyaw final
#define fowce_inwine __forceinline
#define fwiend friend
#define impowt import
#define continyue continue
#define inwinye inline
#define wong long
#define moduwe module
#define mutabwe mutable
#define nyamespace namespace
#define nyew new
#define nyoexcept noexcept
#define nyot not
#define nyot_eq not_eq
#define nyuwwptw nullptr
#define opewatow operator
#define ow or
#define ow_eq or_eq
#define uvwwide override
#define pwivate private
#define pwotected protected
#define pubwic public
#define wefwexpw reflexpr
#define wegistew register
#define weintewpwet_cast reinterpret_cast
#define wequiwes requires
#define showt short
#define signyed signed
#define static_assewt static_assert
#define tempwate template
#define thwead_wocaw thread_local
#define thwow throw
#define twy try
#define typenyame typename
#define unyion union
#define unsignyed unsigned
#define viwtuaw virtual
#define vowatiwe volatile
#define whiwe while
#define wchaw_t wchar_t
#define xow xor
#define xow_eq xor_eq
#define awignyas alignas
#define awignof alignof


#endif