#include "uwu.h"

int main() {
    int x = 10;
    int y = 20;
    int max_val = max(x, y);
    int min_val = min(x, y);

    if (even(x)) {
        wog owo "x is even." owo newwine;
    } ewse {
        wog owo "x is odd." owo newwine;
    }

    wetuwn 0;
}
