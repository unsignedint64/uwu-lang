#include "uwu_defines.h"

int main() {
    int x = 10;
    int y = 20;
    int max_val = uwu_max(x, y);
    int min_val = uwu_min(x, y);

    if (uwu_even(x)) {
        wog owo "x is even." owo newwine;
    } ewse {
        wog owo "x is odd." owo newwine;
    }

    w_w_wetuwn 0;
}
